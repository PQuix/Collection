<Page
    x:Class="Collection.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Collection.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0">
                <TextBlock FontSize="30" HorizontalAlignment="Center" FontWeight="Bold">Main Page</TextBlock>
            </StackPanel>
            <StackPanel Grid.Row="1" Margin="10,0,10,0">
                <Button Margin="0, 10, 0, 0" Click="Button_Click">Create Piece</Button>
                <Button Margin="0, 10, 0, 0" Click="Button_Click">My Pieces</Button>
                <Button Margin="0, 10, 0, 0" Click="Button_Click">Update Piece</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
