<Page
    x:Class="Collection.UWP.DisplayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Collection.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:Collection.Model"
    mc:Ignorable="d">

    <Page.Resources>
        <local:DesignData x:Key="DesignData"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>
        <RelativePanel Grid.Column="0" Grid.RowSpan="4">
            <Rectangle Name="MenuRow" Fill="DarkGray" Stretch="UniformToFill"/>
        </RelativePanel>
        <RelativePanel Grid.Row="2" Grid.Column="1" MinWidth="300">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="50,0,50,0" Name="displayGrid">
                    <StackPanel d:DataContext="{StaticResource DesignData}">
                        <GridView ItemsSource="{Binding Pieces}" Name="ActGrid" SelectionChanged="ActGrid_SelectionChanged">
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <RelativePanel Margin="20,0,20,0" Height="300">
                                        <Image Name="GridImage" RelativePanel.AlignTopWithPanel="True" RelativePanel.AlignLeftWithPanel="True" Width="150" Source="{Binding PieceCover}"/>
                                        <TextBlock Name="GridTitle" FontSize="18" TextWrapping="Wrap" RelativePanel.Below="GridImage" RelativePanel.AlignLeftWithPanel="True" Text="{Binding PieceTitle}"/>
                                        <TextBlock Name="GridAuthor" FontSize="15" TextWrapping="Wrap" RelativePanel.Below="GridTitle" RelativePanel.AlignLeftWithPanel="True" Text="{Binding PieceAuthor}"/>
                                    </RelativePanel>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                        </GridView>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </RelativePanel>
        <RelativePanel Grid.Row="2" Grid.Column="2" MaxWidth="250" MinWidth="188">
            <TextBlock Name="Title" FontSize="20" FontStyle="Italic" TextWrapping="Wrap"/>
            <TextBlock Name="Author" TextWrapping="Wrap" RelativePanel.Below="Title"/>
            <TextBlock Name="Description" TextWrapping="Wrap" RelativePanel.Below="Isbn"/>
            <Image Name="Cover" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="Author" MaxHeight="400" MinHeight="300"/>
            <TextBlock Text="ISBN:" RelativePanel.Below="Cover" RelativePanel.AlignLeftWithPanel="True"/>
            <TextBlock Name="Isbn" TextWrapping="Wrap" RelativePanel.Below="Cover" RelativePanel.AlignRightWith="Cover"/>
            <Button Name="Update" RelativePanel.AlignLeftWithPanel="True" RelativePanel.Below="Description" Click="Button_Click">Update Piece</Button>
            <Button Name="Delete" Margin="10,0,0,0" RelativePanel.RightOf="Update" RelativePanel.Below="Description" Click="Button_Click">Delete Piece</Button>
        </RelativePanel>
    </Grid>
</Page>
